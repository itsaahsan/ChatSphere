import React from 'react';

test('ChatSphere app loads', () => {
  expect(true).toBe(true);
});

describe('Basic App Test', () => {
  it('should pass basic test', () => {
    const app = 'ChatSphere';
    expect(app).toBe('ChatSphere');
  });
});
